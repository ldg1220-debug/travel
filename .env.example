# Google Maps JavaScript API key (client-exposed). Without this the app
# renders an offline decorative fallback map so Phase 2/3 UX still works.
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=

# Google Places API (New) key used by the data pipeline (src/server/pipeline)
# to resolve scraped place names to place_id/lat/lng/rating.
# Without this the pipeline resolves against an offline fixture instead.
GOOGLE_PLACES_API_KEY=

# Optional: LLM key used by the pipeline's authenticity check
# (src/server/pipeline/llmVerifier.ts). Without this a conservative
# heuristic is used instead.
LLM_API_KEY=
